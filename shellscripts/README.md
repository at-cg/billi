#### 1. convert.sh
For converting FILE.vg to FILE.gfa

#### 2. compare.sh
Script for comparing the output of the tools

#### 3. annotate.sh
For annotating the variants in the underlying bubbles

#### 4. modify_GFA.sh
For modifying the GFA file for input to pangene

#### 4. compare_output.sh
For comparing the output of different tools

